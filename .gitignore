__pycache__/
*.py[cod]
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local overrides: config.toml in the working directory shadows the real
# user config, which is handy for development but must not be committed.
/config.toml

# Personal notes: drafts, published texts, things that are about the project
# rather than part of it.
/notes.md
